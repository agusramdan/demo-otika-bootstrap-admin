<?xml version="1.0"?>
<thlogic>
    <attr sel="#domainData"
          method="post"
          role="form"
          data-th-object="${domain}"
          data-th-action="@{/idm/users/save}" >
        <attr sel="#username" data-th-field="*{id}" />
        <attr sel="#firstName" data-th-field="*{firstName}" />
        <attr sel="#lastName" data-th-field="*{lastName}" />
        <attr sel="#enabled" data-th-field="*{enabled}" />
    </attr>
</thlogic>